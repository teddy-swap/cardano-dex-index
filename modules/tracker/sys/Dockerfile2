
#docker run -entrypoint="/bin/bash" -e "/home/apps/tracker-scala/conf/app.conf" --restart=always --name=tracker-scala --network="dev" -d timooxaaa/tracker-spectrum:0.0.1

docker run \
    -v /home/apps/tracker-scala/conf/app.conf:/cardano-analytics-scala/conf/conf.env:ro \
    --restart=always \
    --network="dev" \
    --name=tracker-scala \
    -d timooxaaa/tracker-spectrum:0.0.1

sudo docker build -t timooxaaa/tracker-spectrum:0.0.1 -f modules/tracker/sys/Dockerfile .